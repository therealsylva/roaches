# MovieBoxxed

Reproducible Android binary-cleanup project for the community-maintained
MovieBox v4.0 artifact.

The project removes advertising, unnecessary telemetry, invasive permissions,
and unused components while preserving the functionality already accessible in
the supplied community build.

Development happens through reviewed pull requests. Upstream APKs and signing
keys are never committed to Git.
